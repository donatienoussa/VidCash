import { Image, StyleSheet, Platform, Text, Button } from 'react-native';


export default function HomeScreen() {
  return (
    <>
      <Text className="flex justify-center items-center text-3xl font-bold">Modif dans dev</Text>
      <Button title="Select a photo" onPress={() => {}} />
    </>
  );
}
